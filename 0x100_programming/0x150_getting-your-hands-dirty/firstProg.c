# include <stdio.h>

int main()
{
    int i;
    for (i = 0; i < 10; i++)          // Loop ten times
    {
        printf("Hello, world!\n");    // Put the string to the output
    }
    return 0;                         // Tells OS the program exited with no errors
}